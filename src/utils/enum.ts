export enum UserTypes {
  FullAccess = "full_access",
  Operator = "operator"
}

export enum SessionType {
  Training = "training",
  Match = "match",
}

export enum TrainingDifficulty {
  Easy = "easy",
  Medium = "medium",
  Hard = "hard",
}

